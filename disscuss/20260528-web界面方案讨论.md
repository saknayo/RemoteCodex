# 20260528 Web界面方案讨论

## 2026-05-28 13:40 - 防攻击方案定稿

### 用户需求
- 登录间隔30秒限制
- 确认防攻击策略汇总

### 讨论结论

**防攻击策略汇总：**
| 限制 | 参数 | 说明 |
|------|------|------|
| 登录间隔 | 30秒 | 两次登录必须间隔30秒 |
| 失败封禁 | 3次/30分钟 | 3次失败封禁IP 30分钟 |
| 速率限制 | 10次/分钟 | 单位时间请求次数限制 |
| 计数窗口 | 15分钟 | 失败计数15分钟后重置 |

**技术栈：** Node.js + Express + Socket.io

**功能范围：**
- 简单对话
- 会话历史
- 会话内容渲染

**安全机制：**
- 用户名+密码认证
- JWT Token
- bcrypt 密码哈希
- IP封禁
- 登录间隔限制

---

## 2026-05-28 13:30 - 加入登录间隔限制

### 用户需求
- "每次登录间隔30秒"

### 讨论结论

加入登录间隔限制，更新 SECURITY_CONFIG：
```javascript
const SECURITY_CONFIG = {
  LOGIN_INTERVAL: 30,           // 最小登录间隔（秒）
  MAX_ATTEMPTS: 3,              // 最大失败次数
  BLOCK_DURATION: 30 * 60,      // 封禁时长（秒），30分钟
  ATTEMPT_WINDOW: 15 * 60,      // 尝试计数窗口（秒），15分钟
  RATE_LIMIT_WINDOW: 60,        // 速率限制窗口（秒）
  RATE_LIMIT_MAX: 10,           // 窗口内最大请求数
  CLEANUP_INTERVAL: 60 * 60     // 清理过期数据间隔（秒），1小时
};
```

前端需显示倒计时等待界面。

---

## 2026-05-28 13:20 - 加入IP封禁防攻击

### 用户需求
- "带登录密码功能吧，要用户名+密码强保护"
- "同时还要有防攻击，登录3次失败直接封禁ip，"

### 讨论结论

**认证系统升级：**
1. 用户名+密码认证（替代简单密码）
2. JWT Token 机制
3. bcrypt 密码哈希存储
4. IP封禁机制：3次失败封禁30分钟

**封禁存储结构（bans.json）：**
```json
{
  "192.168.1.100": {
    "attempts": 3,
    "blockedAt": "2026-05-28T10:30:00Z",
    "expiryAt": "2026-05-28T11:00:00Z",
    "reason": "login_failed"
  }
}
```

---

## 2026-05-28 13:10 - 初始需求沟通

### 用户需求
1. 技术栈：稳定的，无明显偏好
2. 功能：简单对话、会话历史、会话内容渲染
3. 部署：个人使用、密码保护、公网、服务器在内网但有公网跳板
4. 环境：Linux、命令行调用CLI

### 讨论结论

**推荐方案：** Node.js + Express + Socket.io
- 与 Claude CLI 同栈，兼容性好
- WebSocket 支持流式输出
- 简单稳定

**功能设计：**
- 密码认证（后升级为用户名+密码）
- 会话管理
- 流式输出
- 内容渲染

**部署架构：**
```
公网跳板机(Nginx) → 内网服务器(Web Server + CLI)
```
