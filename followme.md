## 必须遵守的规则

1. 禁止暴露隐私信息，包括但不限于公网 IP、内网敏感地址、用户名、密码、token、API key、SSH key、JWT 密钥、证书私钥和其他认证凭据。
2. 禁止将隐私信息写入可提交文件、讨论归档、日志归档、commit message 或其他会进入 git 历史的内容。
3. 提交、推送到远端仓库、归档讨论记录、输出命令示例或编写文档前，必须检查相关内容是否包含隐私信息。
4. 需要保存本机私有配置时，只能写入已被 `.gitignore` 忽略的本地文件，例如 `.env`，并在可提交文档中使用占位符或泛化描述。
